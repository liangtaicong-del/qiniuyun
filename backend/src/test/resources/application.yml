spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  h2:
    console:
      enabled: false

app:
  jwt:
    secret: test-secret-key-for-unit-testing-at-least-32-characters
    expiration: 86400000
  admin:
    username: admin
    password: admin123
    email: admin@test.com
